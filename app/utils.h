#include <stdbool.h>
#include "sys/rtimer.h"

//void log_nd(unsigned short node_id, uint8_t epoch, rtimer_clock_t timestamp);
//void log_epoch(uint8_t epoch, rtimer_clock_t timestamp);
bool contains(uint8_t array[],uint8_t length, uint8_t toBeSearched);